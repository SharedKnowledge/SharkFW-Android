<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.sharksystem.sharkfw_android">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name="net.sharksystem.android.protocols.routing.service.SharkServiceAutoStartReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--<service-->
            <!--android:name="net.sharksystem.android.protocols.routing.serviceOLD.LocationServiceOld"-->
            <!--android:enabled="true" />-->

        <service
            android:name="net.sharksystem.android.protocols.routing.location_to_be_implemented.LocationService"
            android:enabled="true" />

        <service android:name="net.sharksystem.android.protocols.routing.service.RoutingService" android:enabled="true"/>

        <!--<receiver android:name="net.sharksystem.android.protocols.routing.serviceOLD.LocationReceiverOld" android:process=":remote" >-->

            <!--&lt;!&ndash; this Broadcast Receiver only listens to the following intent &ndash;&gt;-->
            <!--<intent-filter>-->
                <!--<action android:name="org.mabna.order.ACTION_REFRESH_SCHEDULE_ALARM" />-->
            <!--</intent-filter>-->
        <!--</receiver>-->



    </application>

</manifest>